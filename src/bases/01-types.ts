export let name: string = 'Kenner';
export const age = 35;
export const isValid: boolean = true;

name = 'Sofía';

export const templateString = `Hola, soy ${name} y tengo ${age} años`;

console.log(templateString);
